rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
// SOLO PARA PRUEBAS. Antes de usar datos reales, activa Authentication
// y cambia estas reglas a request.auth != null.
