rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
// SOLO PARA PRUEBAS. Antes de usar datos reales, protege Storage con Authentication.
